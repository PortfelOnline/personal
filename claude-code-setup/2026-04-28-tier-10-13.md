# Claude Code Stack Upgrade — Tier 10 → 13

**Дата**: 2026-04-28
**Ветка**: main
**Модель**: deepseek-v4-pro (reasoning) + claude-sonnet-4-6 (executor)

## Сводка улучшений

| # | Категория | Что сделано | Файлы |
|---|-----------|-------------|-------|
| 1 | **Keybindings** | `ctrl+k t/f/o` (терминал/поиск/quickOpen) + `ctrl+k m` (экшены сообщений) | `~/.claude/keybindings.json` |
| 2 | **Remote Triggers** | 2 облачных cron-триггера: nightly-cleanup (22:57 UTC) + weekly-refs-update (Пн 04:57 UTC) | `claude.ai/code/routines` |
| 3 | **Scheduled Tasks** | 2 сессионные cron-задачи: очистка логов (3:57 IST) + refs update (Пн 9:57 IST) | in-memory |
| 4 | **check-secrets hook** | Подключён как PreToolUse перед `git commit` — проверка паролей/токенов/API-ключей в staged файлах | `settings.json` |
| 5 | **Statusline** | `main \| sonnet \| 15t \| *4` — git-ветка, модель, TODO-файлы, незакоммиченные изменения | `settings.json` |
| 6 | **Кастомные команды** | `/health-check` — MCP/серверы/БД; `/deploy-kad` — деплой на production | `~/.claude/commands/` |
| 7 | **settings.local.json** | 117KB → 38KB (−67%), 983 → 586 правил, убран пароль из sshpass | `settings.local.json` |
| 8 | **Агенты автообнаружение** | 29 симлинков из поддиректорий → top-level `.md` | `~/.claude/agents/` |

## Remote Triggers (облачные)

| Триггер | ID | Cron (UTC) | Статус |
|---------|-----|-----------|--------|
| nightly-cleanup | `trig_01TSsUhTqR3SApnbDJi7mvuD` | `57 22 * * *` | enabled |
| weekly-refs-update | `trig_01AUM2do1K1q4DjHPSUMG868` | `57 4 * * 1` | enabled |
| nightly-cleanup (old) | `trig_01XTzVyqPVeowjwgV68VfdFa` | none | **disabled** |

**Важно**: промпты и MCP-коннекторы настраиваются через веб-интерфейс `claude.ai/code/routines`. CLI-инструмент RemoteTrigger — только базовый CRUD.

## Безопасность

- Пароль `sshpass` удалён из `settings.local.json` (был в 117 правилах)
- Вместо `sshpass -p '...'` → `ssh audioceh` (SSH-ключ `~/.ssh/id_ed25519`)
- `check-secrets.sh` теперь срабатывает перед каждым `git commit`

## Агенты — новые симлинки

Созданы симлинки в `~/.claude/agents/` для 29 агентов из поддиректорий:
`build-tools-vite-expert`, `build-tools-webpack-expert`, `code-quality-linting-expert`,
`database-expert`, `database-mongodb-expert`, `database-postgres-expert`,
`devops-expert`, `documentation-expert`, `e2e-playwright-expert`,
`framework-nextjs-expert`, `frontend-accessibility-expert`, `frontend-css-styling-expert`,
`git-expert`, `docker-reviewer`, `iac-reviewer`, `infrastructure-docker-expert`,
`infrastructure-github-actions-expert`, `nginx-reviewer`, `kafka-expert`,
`loopback-expert`, `nodejs-expert`, `react-expert`, `react-performance-expert`,
`refactoring-expert`, `jest-testing-expert`, `testing-expert`, `vitest-testing-expert`,
`typescript-build-expert`, `typescript-expert`

## Конфигурация хуков (settings.json)

| Событие | Хуки |
|---------|------|
| PreToolUse | pre-edit-guard, destructive-guard, **check-secrets** (new) |
| PostToolUse | post-edit-lint, validate-agents, post-commit-graphify |
| SessionStart | session-start-context |
| Stop | session-save-memory, session-report |
| PostToolUseFailure | post-tool-failure |
| PreCompact | pre-compact-checkpoint |
| PostCompact | post-compact-restore |
| UserPromptSubmit | user-prompt-submit |
| Notification | notification |

## Tier 14 — ЗАКРЫТ (Cloud Code → DeepSeek)

- [x] **Авто-push конфигурации**: хук `auto-push-config.sh` на Stop — коммитит и пушит `.claude/` изменения в `PortfelOnline/kadmap` main
- [x] **Промпты в Remote Triggers** → заменены на launchd + DeepSeek (`com.local.nightly-cleanup` + `com.local.refs.update`)
- [x] **MCP-коннекторы к триггерам** → не нужны: локальные скрипты имеют прямой доступ к инструментам (git, find, graphify)
- [x] **Удалить старый тестовый триггер** → переименован в `DEPRECATED-replaced-by-deepseek-local`, disabled, cron cleared

**Решение**: Cloud Code отключён на Free плане → все scheduled tasks перенесены на launchd + локальные скрипты + DeepSeek API.

### Замены Cloud Code → DeepSeek

| Cloud Code триггер | Замена | Статус |
|---------------------|--------|--------|
| `nightly-cleanup` (22:57 UTC) | `com.local.nightly-cleanup` launchd (3:57 IST) → `~/.local/bin/nightly-cleanup.sh` | ✅ активно |
| `weekly-refs-update` (Пн 4:57 UTC) | `com.local.refs.update` launchd (Пн 9:00 IST) → `~/.claude/scripts/update-refs.sh` + graphify | ✅ активно |
| `nightly-cleanup` (old, disabled) | Переименован в `DEPRECATED-replaced-by-deepseek-local` | ✅ помечен |

### auto-push-config.sh

- Срабатывает на **Stop** (конец сессии)
- Проверяет изменения в `~/.claude/`
- Auto-commit с timestamp + push в `origin/main`
- timeout: 30s, с retry-логикой при git lock

### Новые файлы Tier 14

| Файл | Назначение |
|------|-----------|
| `~/.local/bin/nightly-cleanup.sh` | Ежедневная очистка логов + проверка диска |
| `~/.local/bin/weekly-refs-update.sh` | Еженедельное обновление refs + graphify |
| `~/Library/LaunchAgents/com.local.nightly-cleanup.plist` | launchd расписание (3:57 IST) |

## Tier 15 — DeepSeek API (прямой доступ)

- [x] **Библиотека** `~/.local/lib/deepseek_api.py` — `chat()`, `stream_chat()`, `list_models()`
- [x] **CLI** `~/.local/bin/deepseek` — streaming/non-streaming, JSON mode, system prompt, temperature
- [x] **Переменные** `DEEPSEEK_API_KEY` + `PYTHONPATH` в `.bashrc` и `.zshrc`
- [x] **Проверено**: обе модели (`v4-pro`, `v4-flash`) работают через OpenAI-совместимый эндпоинт
- [x] **Cloud Code триггеры заменены** на launchd + локальные скрипты (Tier 14)

### Файлы
| Файл | Назначение |
|------|-----------|
| `~/.local/lib/deepseek_api.py` | Библиотека |
| `~/.local/bin/deepseek` | CLI |
| `~/.bashrc` | env vars |
| `~/.zshrc` | env vars |
| `personal/claude-code-setup/2026-04-28-deepseek-api.md` | Документация |
| `personal/claude-code-setup/2026-04-28-tier-10-13.md` | Этот документ |

DeepSeek API теперь работает независимо от Claude Code — стек может функционировать без API Claude Code.
